task:
  n_classes: 4
  cue_steps: 5
  delay_steps: 20
  delay_steps_min: 15
  delay_steps_max: 45
  response_steps: 5
  batch_size: 64
  seed: 20260629

model:
  hidden_size: 64
  dt: 20.0
  tau: 100.0

training:
  steps: 2000
  learning_rate: 0.001
  log_every: 100
  device: auto
  score_delay_period: true

evaluation:
  batches: 20

analysis:
  n_components: 2
  n_trials: 64

paths:
  output_dir: outputs/baseline_delay_stable
  run_name: baseline_delay_stable
