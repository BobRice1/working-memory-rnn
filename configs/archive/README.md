# Archived Configurations

These YAMLs describe superseded development runs retained for provenance:

- `tuned_delay_stable.yaml`: intermediate circular attractor-oriented model.
- `tuned_delay_fixation_gate.yaml`: fixed-delay Yang-style precursor.

Older categorical YAMLs are recoverable from Git history at `408e824^`. The
response-gated prototype configuration is embedded in its checkpoint. See
`docs/model-run-archive.md` for the complete archive map.
